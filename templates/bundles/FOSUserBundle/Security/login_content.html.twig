{% trans_default_domain 'FOSUserBundle' %}

{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}

<div class="wrapper">
    <form class="form" action="{{ path("fos_user_security_check") }}" role="form" autocomplete="off" id="formLogin" novalidate="" method="POST">
        <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />

        {% if csrf_token %}
            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
        {% endif %}
        <div class="form-group">
            {#<label for="_username">Username</label>#}
            <input type="text" class="form-control form-control-lg rounded-0" name="_username" 
                placeholder="{{ 'security.login.username'|trans({}, 'FOSUserBundle')  }}" id="username" required="required">
                {#<div class="invalid-feedback">Oops, you missed this one.</div>#}
        </div>
        <div class="form-group">
            {#<label>Password</label>#}
            <input type="password" class="form-control form-control-lg rounded-0" id="password" name="_password" 
                    placeholder="{{ 'security.login.password'|trans({}, 'FOSUserBundle')  }}" required="required" autocomplete="new-password">
            {#<div class="invalid-feedback">Enter your password too!</div>#}
        </div>


        <div class="form-group">
            <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
            <label for="remember_me">{{ 'security.login.remember_me'|trans({}, 'FOSUserBundle') }}</label>
                {#<input type="checkbox" class="custom-control-input" id="loginRemember">
                <label class="custom-control custom-checkbox" for="loginRemember">Souvenez-vous de moi</label>#}
        </div>
        {#<button type="submit" class="btn btn-success btn-lg float-right" id="btnLogin">Connection</button>#}

        <input type="submit" id="_submit" class="btn btn-lg btn-primary btn-block" name="_submit" value="{{ 'security.login.submit'|trans({}, 'FOSUserBundle')  }}" />

        <div class="link_to_change">
            <a href="{{ path('fos_user_registration_register')}}">S'enregistrer</a> - <a
                href="{{ path('fos_user_resetting_request')}}">Mot de passe oublié</a>
        </div>
    </form>
    {#<form action="{{ path("fos_user_security_check") }}" method="post" class="form-signin">
        <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
        <h2 class="form-signin-heading">Enregistrement de votre compte :</h2><br>

        {% if csrf_token %}
            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
        {% endif %}

        <input type="text" id="username" name="_username" class="form-control" value="{{ last_username }}" 
            placeholder="{{ 'security.login.username'|trans({}, 'FOSUserBundle')  }}" required="required" autocomplete="username" autofocus="autofocus"/>

        <input type="password" id="password" name="_password" class="form-control" 
            placeholder="{{ 'security.login.password'|trans({}, 'FOSUserBundle')  }}" required="required" autocomplete="current-password"/>

        <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
        <label for="remember_me">{{ 'security.login.remember_me'|trans({}, 'FOSUserBundle') }}</label>

        <input type="submit" id="_submit" class="btn btn-lg btn-primary btn-block" name="_submit" value="{{ 'security.login.submit'|trans({}, 'FOSUserBundle')  }}" />

        <div class="link_to_change">
            <a href="{{ path('fos_user_registration_register')}}">S'enregistrer</a> - <a
                href="{{ path('fos_user_resetting_request')}}">Mot de passe oublié</a>
        </div>
    </form>#}
</div>

</div>
</div>
</div>
</div>
</div>
